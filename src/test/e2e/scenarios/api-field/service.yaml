---
apiVersion: v1
kind: Service
metadata:
  name: api-field
  namespace: validation-test
  labels:
    app: api-field
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: api-field
